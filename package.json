{
  "name": "ethara-assignment",
  "private": true,
  "scripts": {
    "build": "npm install --prefix frontend --legacy-peer-deps && npm install --prefix backend --legacy-peer-deps && npm run build --prefix frontend && npm run build --prefix backend && mkdir -p backend/public && cp -r frontend/dist/public/* backend/public/",
    "start": "npm start --prefix backend"
  }
}
