{
  "name": "ethara-assignment",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm install --prefix frontend && npm install --prefix backend && npm run build --prefix frontend && npm run build --prefix backend && mkdir -p backend/public && cp -r frontend/dist/public/* backend/public/",
    "start": "npm start --prefix backend"
  }
}
